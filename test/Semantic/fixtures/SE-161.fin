task class TaskClass0 {

    in0 : in u16 triggers action0;
    
    action action0(&priv self, _input : u16) -> Status<i32> {

        var ret : Status<i32> = Success;

        return ret;

    }

};

channel msg_queue0 : MsgQueue<u32; 10>;
task task0 : TaskClass0 = { in0 <- msg_queue0 };
