enum Enum0 {

    variant0,
    variant1,
    variant2,
    variant3

};

enum Enum1 {

    variant0

};

function foo(input : Enum0) -> u32 {

    var ret : u32 = 0;

    if input is Enum1::variant0 {
        ret = 1;
    }

    return ret;

}
