function bar(slice : &[u32; 2]) -> u32 {

    return slice[1];

}

function foo() -> u32 {

    let lower_bound : u32 = 1;
    let upper_bound : usize  = 3;

    let array0 : [u32; 5] = { 0, 1, 2, 3, 4 };

    return bar(&array0[lower_bound..upper_bound]);

}
