struct TMDescriptorT {
    tm_bytes : [u8 ; 256];
    tm_num_bytes : u16;
};

interface Test {

    procedure test (&mut self, testD : box TMDescriptorT);

};

resource class TMChannel provides Test {

    tm_pool : access Allocator<TMDescriptorT>;

   procedure test (&mut self, testD : box TMDescriptorT){
             self->tm_pool.free(testD);
             return;
   }
};
